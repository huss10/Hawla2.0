[build]
buildCommand = "npm install && npm rebuild better-sqlite3 && npx playwright install chromium --with-deps"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3